@model CyberHub.ViewModels.CommentViewModel

<div class="comment" style="padding: 8px; border-bottom: 1px solid #333;">
    <strong>@Model.AuthorName</strong> •
    <small>@Model.CreatedAt.ToString("MMM dd, HH:mm")</small>
    <p>@Model.Content</p>
</div>
